<!DOCTYPE html>
<html>
  <head>
    <title></title>
    <%= csrf_meta_tags %>
    <%= stylesheet_link_tag    'application'%>
    <%= javascript_include_tag 'application' %>
  </head>

  <body>
    <%= render 'layouts/header'%>
     <div class="container">
      <% flash.each do |message_type, message| %> <!--flash = { success: "It worked!", danger: "It failed." }  message type is the key and message is the value-->
        <div class="alert alert-<%= message_type %> mt-2"><%= message %></div>
      <% end %>
        <%= yield %>
        
    </div
  </body>
</html>
